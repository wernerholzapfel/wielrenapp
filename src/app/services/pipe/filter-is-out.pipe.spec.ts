import { FilterIsOutPipe } from './filter-is-out.pipe';

describe('FilterIsOutPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterIsOutPipe();
    expect(pipe).toBeTruthy();
  });
});
