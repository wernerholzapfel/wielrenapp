import { SearchRidersPipe } from './searchRiders.pipe';

describe('SearchridersPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchRidersPipe();
    expect(pipe).toBeTruthy();
  });
});
